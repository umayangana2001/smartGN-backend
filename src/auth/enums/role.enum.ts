// Role enum - mirrors Prisma schema
// This will be replaced by @prisma/client after running: npx prisma generate
export enum Role {
  USER = 'USER',
  VILLAGE_OFFICER = 'VILLAGE_OFFICER',
  ADMIN = 'ADMIN',
}
